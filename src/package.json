{
  "name": "nexroute",
  "version": "4.0.0",
  "description": "NexRoute — Express server for Google Cloud Run, Firestore-backed storage (Google-native, no external database), static frontend served from the same app.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
