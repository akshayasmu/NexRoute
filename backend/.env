# Copy this file to .env (same folder) and fill in what you have.
# NexRoute runs with NONE of these set — it just shows a visible
# "DEMO DATA" badge instead of "LIVE" wherever a key is missing.
# Never commit the real .env file (it's already gitignored).

# 1. LTA DataMall — https://datamall.lta.gov.sg (free, instant registration)
#    Powers: train service alerts, station crowd (real-time + forecast),
#    bus arrival, lift/escalator maintenance, planned bus routes, etc.
LTA_ACCOUNT_KEY=NJ+vkf/7Q+mCJJO/RsgTdQ==

# 2. OneMap — https://www.onemap.gov.sg/apidocs/ (free, instant registration)
#    Powers: the walking route drawn on the OpenStreetMap-based map.
ONEMAP_EMAIL= akshaya.m.2025@computing.smu.edu.sg
ONEMAP_PASSWORD= W63GSW9usf@@yMG

# 3. data.gov.sg weather — no key needed at all, this one just works.

PORT=3000
